/**
 * Every promise we make about timing or availability lives here, so the same
 * words appear on every page. Change it once, it changes everywhere.
 *
 * NB: PROPOSAL_DAYS is the figure the audit recommended in place of the vague
 * "a few days" / "a few business days" pair. Confirm it matches the real
 * turnaround before the next deploy.
 */

/** Full sentence, used in body copy. */
export const REPLY_PROMISE = 'We reply within 24 hours on weekdays.';

/** Compact form for stat cells and meta rows. */
export const REPLY_PROMISE_SHORT = '< 24 hrs';
export const REPLY_PROMISE_QUALIFIER = 'On weekdays';

/** Turnaround for a written proposal after the call. */
export const PROPOSAL_PROMISE = 'You get a written proposal within three business days.';
export const PROPOSAL_WINDOW = 'within three business days';

/** Where we work. Kept to what the portfolio actually evidences: NL, BE, AT. */
export const AREA_SERVED = 'across the Netherlands, Belgium and beyond';
